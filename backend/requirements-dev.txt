# CodeAcademy Pro — Dependencias de desarrollo y pruebas.
#
#   pip install -r requirements-dev.txt
#
# No entran en la imagen de producción (docker/Dockerfile.backend instala solo
# requirements.txt): correr los tests en el contenedor no hace falta y engorda la imagen.

-r requirements.txt

# ── Pruebas ──────────────────────────────────────────────────────
pytest==8.3.0
pytest-asyncio==0.24.0

# ── Lint ─────────────────────────────────────────────────────────
ruff==0.6.0
